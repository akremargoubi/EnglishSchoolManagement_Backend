apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: esm
spec:
  selector:
    app: gateway-service
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
