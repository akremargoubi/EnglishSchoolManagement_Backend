# ============================================================
# DATABASE (Docker container names)
# ============================================================
spring.datasource.url=jdbc:postgresql://auth-db:5432/esmauthdb
spring.datasource.username=authserviceuser
spring.datasource.password=authservicedbp4ss

# ============================================================
# EUREKA (Docker container name)
# ============================================================
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka

# ============================================================
# EMAIL (Docker container name)
# ============================================================
email.service.url=http://email-service:2001/api/emails/send

# ============================================================
# STORAGE — local fallback
# ============================================================
app.storage.avatar-dir=/app/avatars

# ============================================================
# S3 / B2 Cloud Storage (override via Docker env vars)
# ============================================================
app.storage.s3.enabled=${S3_ENABLED:true}
app.storage.s3.endpoint=${S3_ENDPOINT:https://s3.eu-central-003.backblazeb2.com}
app.storage.s3.region=${S3_REGION:eu-central-003}
app.storage.s3.bucket=${S3_BUCKET:englishschoolstorage}
app.storage.s3.access-key=${S3_ACCESS_KEY:0037e4e13ac65740000000001}
app.storage.s3.secret-key=${S3_SECRET_KEY:K003KRjjmSiAbknHEeqoWCRrlgxTgqA}
app.storage.s3.public-url-prefix=${S3_PUBLIC_URL_PREFIX:https://f003.backblazeb2.com/file/englishschoolstorage}