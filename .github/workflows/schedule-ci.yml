name: Schedule Service CI

on:
  push:
    paths:
      - 'schedule-service/**'
    branches: [ rayen ]
  pull_request:
    paths:
      - 'schedule-service/**'
    branches: [ rayen ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
      
      - name: Build and test
        run: |
          cd schedule-service
          mvn clean test
      
      - name: Run SonarQube analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          cd schedule-service
          mvn sonar:sonar -Dsonar.projectKey=schedule-service -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=$SONAR_TOKEN
      
      - name: Build JAR
        run: |
          cd schedule-service
          mvn package -DskipTests
      
      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: schedule-service-jar
          path: schedule-service/target/*.jar